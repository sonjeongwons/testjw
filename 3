[Nginx 캐싱]
클라이언트 → Nginx (캐시 있음) → 바로 응답
                            → (캐시 없음) → API 서버 호출 후 응답 캐싱

[Redis 캐싱]
클라이언트 → API 서버 → Redis (캐시 있음) → 바로 응답
                              → (캐시 없음) → DB 호출 후 Redis에 저장
